//app.post("/upload", upload.single("foto"), (req, res) => {
    //const fs = require("fs");

    //const { nome, localizacao, informacoes } = req.body;
    //const imageBuffer = fs.readFileSync(req.file.path);

    //const sql = "INSERT INTO lugares (nome, localizacao, informacoes, foto) VALUES (?, ?, ?, ?)";
    //db.query(sql, [nome, localizacao, informacoes, imageBuffer], (err, result) => {
        //if (err) throw err;
        //res.send("Imagem salva no banco!");
    //});
//});
